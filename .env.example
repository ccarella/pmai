# OpenAI API Configuration
OPENAI_API_KEY=

# Rate Limiting Configuration
RATE_LIMIT_REQUESTS_PER_HOUR=20
RATE_LIMIT_MAX_TOKENS_PER_REQUEST=2000

# Cost Protection
MAX_MONTHLY_COST_USD=10

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NextAuth.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=# Generate with: openssl rand -base64 32

# GitHub OAuth Configuration
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Upstash Redis Configuration (from Vercel)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
REDIS_URL=

# Database URL for Prisma (using Redis)
DATABASE_URL=

# Encryption key for sensitive data (REQUIRED)
# Generate with: npm run generate-key
# This key is used to encrypt user API keys stored in the database
# IMPORTANT: Keep this key secure and use the same key across all instances
# WARNING: Changing this key will make existing encrypted data unrecoverable
ENCRYPTION_KEY=